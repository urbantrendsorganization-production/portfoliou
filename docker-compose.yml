services:
  # ── PostgreSQL ──
  portfoliou-db:
    image: postgres:16-alpine
    container_name: portfoliou-db
    restart: unless-stopped
    volumes:
      - portfoliou_postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5435:5432"
    env_file: .env.production
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-portfoliou}
      POSTGRES_USER: ${POSTGRES_USER:-portfoliou}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env.production}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-portfoliou}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - portfoliou-net

  # ── Redis ──
  portfoliou-redis:
    image: redis:7-alpine
    container_name: portfoliou-redis
    restart: unless-stopped
    ports:
      - "127.0.0.1:6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - portfoliou-net

  # ── Django Backend ──
  portfoliou-backend:
    build:
      context: ./backend
    image: portfoliou-backend
    container_name: portfoliou-backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8002:8000"
    volumes:
      - portfoliou_media_data:/app/media
    env_file: .env.production
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-portfoliou}:${POSTGRES_PASSWORD}@portfoliou-db:5432/${POSTGRES_DB:-portfoliou}
      REDIS_URL: redis://portfoliou-redis:6379/0
      # Fixes the 301 Redirect Loop by telling Django it's behind HTTPS
      SECURE_PROXY_SSL_HEADER: HTTP_X_FORWARDED_PROTO,https
      DEBUG: "False"
    depends_on:
      portfoliou-db:
        condition: service_healthy
      portfoliou-redis:
        condition: service_healthy
    networks:
      - portfoliou-net

  # ── Next.js Frontend ──
  portfoliou-frontend:
    build:
      context: .
      args:
        # These are injected during 'docker build' - essential for Next.js
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
        NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-https://portfoliou.urbantrends.dev}
        NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${NEXT_PUBLIC_GOOGLE_CLIENT_ID}
    image: portfoliou-frontend
    container_name: portfoliou-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file: .env.production
    environment:
      BACKEND_INTERNAL_URL: http://portfoliou-backend:8000/api
      NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${NEXT_PUBLIC_GOOGLE_CLIENT_ID}
    depends_on:
      - portfoliou-backend
    networks:
      - portfoliou-net

  # ── Nginx Proxy ──
  portfoliou-nginx:
    image: nginx:alpine
    container_name: portfoliou-nginx
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - portfoliou-frontend
      - portfoliou-backend
    networks:
      - portfoliou-net

volumes:
  portfoliou_postgres_data:
  portfoliou_media_data:

networks:
  portfoliou-net:
    name: portfoliou-net